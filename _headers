https://:project.pages.dev/*
  X-Robots-Tag: noindex

# Static portrait/magazine/project photography is not filename-versioned,
# so a moderate (not "immutable") browser cache bounds staleness risk if a
# file at the same path is ever replaced.
/images/*
  Cache-Control: public, max-age=604800, stale-while-revalidate=86400
